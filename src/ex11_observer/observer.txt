
Определение зависимости "один к многим" между объктами.

Определение зависимости "один к многим" между объктами, таким образо что при изменении сосотояния одного объекта
все зависящие от него обекты были уведомлены об этом и обновились.
